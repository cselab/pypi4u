[MODEL]

Number of model parameters = 3
model file = model
data file = data.txt

[PRIORS]
# Set prior distribution
# prior distributions uniform normal


P1 = uniform 0 5
P2 = uniform 0 5
P3 = uniform 0 5
error prior = normal 0 1

[log-likelihood]
# d_i = f + (alpha * |f|^gamma + b) * sigma * epsilon
# with epsilon ~ N(0,1)
alpha = 0
beta  = 1
gamma = 0
